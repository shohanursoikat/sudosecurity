sudo apt update
sudo apt install -y golang-go git
export GOPATH=$HOME/go
export PATH=$PATH:$GOPATH/bin

# Add those to ~/.bashrc or ~/.zshrc to persist:
echo 'export GOPATH=$HOME/go' >> ~/.zshrc
echo 'export PATH=$PATH:$GOPATH/bin:$PATH' >> ~/.zshrc
source ~/.zshrc
